# Agent Instructions

- Run checks with `./gradlew test` before opening a pull request when feasible.
- Keep commit messages concise and descriptive.
- Prefer Kotlin formatting guided by `./gradlew ktlintCheck` when available.
- Avoid adding unnecessary dependencies without discussion.

These instructions apply to the entire repository.
